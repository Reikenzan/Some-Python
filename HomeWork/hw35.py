"""Write a program that prompts the user to enter a list of names.
Each person's name is separated from the next by a semi-colon (`;')
and the names are entered lastName, firstName. Your program should
then print out the names, one per line, with the first names first
followed by the last names."""


listy = input("Enter a list of names:")

word_list = []
while listy != "":
    # add word to lis
    word_list.append(list)
    for i in word_list:
        print(i, end="")

print("You entered:")
    
